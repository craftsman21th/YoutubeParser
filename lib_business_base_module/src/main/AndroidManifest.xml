<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dubox.drive.basemodule">

  <!-- 定时 Added in API level 31 -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

  <application>

    <meta-data
        android:name="com.moder.compass.base.imageloader.DuboxGlideModule"
        android:value="NetDiskGlideModule" />
    <receiver
        android:name="com.moder.compass.statistics.BackgroundStatAlarmReceiver"
        android:exported="false" />
  </application>

</manifest>
